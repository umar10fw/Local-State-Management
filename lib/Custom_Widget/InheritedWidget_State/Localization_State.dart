import 'package:flutter/cupertino.dart';

class LanguageExample extends StatelessWidget {
  const LanguageExample({super.key});

  @override
  Widget build(BuildContext context) {
    return Text(
      "Hello World", // Normally localized string hota hai
      style: TextStyle(fontSize: 20),
    );
  }
}
